# QA-отчёт

Ручной прогон по всему сайту, 28 августа 2026. Проверено вживую в браузере
на 1440×900 и 375×812, под четырьмя ролями (гость, пациент, врач, клиника).
Блок Back перепроверен после правок бэка в тот же день.

**Не проверялось:** чат и видеозвонок — по договорённости.

---

# Front

## 1. Первая карточка каталога без фото не спасает LCP

**Приоритет:** низкий

1. **Как воспроизвести.** Открыть `/specialists` или `/services` в dev-режиме
   с открытой консолью. Next пишет предупреждение «Image ... was detected as
   the Largest Contentful Paint. Please add the `loading="eager"` property».
2. **Чем мешает.** Самая крупная картинка первого экрана грузится лениво, и
   первая отрисовка контента наступает позже, чем могла бы. На быстром канале
   разница незаметна, на мобильном интернете — заметна.
3. **Приоритет.** Низкий: это метрика скорости, а не сломанный сценарий.
4. **Нужна ли доработка бека.** Нет.
5. **Дополнительно.** Первой карточке `priority` уже проставлен (коммит
   `8d75d74`). Проблема остаётся, когда у первой карточки фото нет: тогда
   LCP-элементом становится соседняя, а какая именно — зависит от данных.
   Правильное решение — помечать `priority` не первую карточку, а первый ряд,
   но на каталогах мобильная и десктопная разметка отрисованы обе, и тогда
   в preload уйдёт восемь картинок вместо одной. Трогать стоит вместе с
   отказом от дублирования разметки, а не отдельно.

## 2. Запись оформляется без выбранной услуги

**Приоритет:** низкий

1. **Как воспроизвести.** `/record` → выбрать специалиста (клиника
   подставится сама), поле «Услуга» не трогать. Календарь разблокируется,
   свободные дни выбираются, форма отправляется.
2. **Чем мешает.** Врач получает запись без указания, на что человек пришёл.
   Поле выглядит как обязательное — стоит рядом с двумя другими и никак не
   помечено как необязательное, — но ни на что не влияет.
3. **Приоритет.** Низкий: запись создаётся и видна в кабинете, теряется
   только услуга.
4. **Нужна ли доработка бека.** Нет — `service_id` в `POST /api/appointments/`
   уже необязателен. Нужно только решить, как правильно, и сделать одинаково:
   либо требовать услугу на фронте, либо подписать поле «необязательно».
5. **Дополнительно.** Проверено на десктопе; в коде `buildAppointmentRequest`
   добавляет `service_id` только при наличии, а `validateAndSubmit` его не
   проверяет — на мобильном будет то же самое.

---

## Исправлено в этом прогоне

Найдено при ручной проверке и уже в мастере:

| Коммит | Что было |
| --- | --- |
| `9dd5315` | Дропдаун открывался с клавиатуры, но пункты были обычными `div` — выбрать значение без мыши было нельзя. Форма с обязательным списком (город в анкете врача) упиралась в тупик. |
| `de9146a` | Закрытая мобильная панель фильтров уезжает трансформом, а не `display:none` — её 13 контролов оставались в порядке табуляции на всех каталогах. Там же: «Сбросить фильтры» показывалась на чистой странице, а у кнопки фильтров не было доступного имени. |
| `efac0e8` | Услугу врача можно было только удалить и завести заново — редактирования не было, хотя `PUT /api/doctor/services/{id}/` бэк поддерживает. |
| `26f5a66` | `/services` на 375px скроллилась по горизонтали: кнопка избранного вылезала за карточку (384px при вьюпорте 375px). |
| `e610a73` | Без выбранного врача календарь заблокирован целиком, но форма писала «Выберите дату и время приёма» — отправляла исправлять то, что исправить нельзя, да ещё и в 700px ниже по странице. Там же подпись «Введите фамилию» → «Фамилия». |
| `febdf38` | «1 совпадений», «1 отзывов», «1 лет опыта» — форма слова была прибита к множественному числу. |
| `85c2c2f` | «Список пуст» и «Нет записей» без пояснений; под фильтром по статусу «Нет записей» читалось как «записей нет вообще». |
| `8d75d74` | Обложки блога (в списке и в статье) и первые карточки каталогов грузились лениво и становились LCP-элементом. |
| `a7a4d14` | В таблице услуг врача колонка называлась «Специализация», а поле в форме — «Категория услуги». |
| `dfdd78a` | Дропдаун внутри модалки на телефоне обрезался: у `Modal` есть `transform` и `overflow:hidden`, из-за которых `fixed`-шторка позиционируется от модалки и режется по ней — пропадали ручка, заголовок и первые пункты. Шторка вынесена в портал. Побочно перестал перекрывать список баннер «Ваш город». |
| `dea86df` | iOS приближал страницу при фокусе в поле со шрифтом меньше 16px и обратно не отдалял. Поиск в чате и телефон в форме записи были 14px. |
| `8aa1a8c` | Услуга врача не привязывалась к клинике. Бэк научился это делать, фронт подключён: дропдаун «Клиника» и одноимённая колонка появляются, когда мест приёма больше одного. |
| `590d026` | Поле «Специалист» на `/record` при ошибке ничем не отличалось от заполненных — теперь подсвечивается красным с подписью, как поля шага 3. |

---

# Back

> Перепроверено 28 августа после правок бэка (привязка услуги к клинике и
> фото услуги). Что закрыто — в конце блока.

## 1. Явный `clinic_id` отклоняется у своей же клиники

**Приоритет:** высокий

Новая валидация `clinic_id` не принимает клинику, к которой врач реально
привязан. Из-за этого весь смысл поля пропадает: врач с несколькими клиниками
не сможет создать услугу ни с одним значением.

Врач 201 (Бегалиев Сейтек) привязан к клинике 199 (ITDos.Clinic) — это видно и
в публичной карточке (`workplaces: [{id: 199}]`), и в том, что бэк **сам**
проставляет эту клинику при создании услуги:

```
POST /api/doctor/services/  {"name":"…","category":"Гепатология"}
→ 201, clinic: {"id":199,"name":"ITDos.Clinic"}          # автоподстановка работает

POST /api/doctor/services/  {"name":"…","category":"Гепатология","clinic_id":199}
→ 400 {"clinic_id":["Вы не привязаны к этой клинике."]}  # та же самая клиника
```

Одинаково для `199` и `"199"`. Чужой `clinic_id` (17) отклоняется правильно —
значит валидатор срабатывает всегда, а не только на чужих.

**Что нужно:** автоподстановка и валидация читают привязку из разных мест —
свести к одному источнику. Проверять стоит там же, откуда клиника берётся при
одной привязке.

**Фронт готов:** дропдаун «Клиника» подключён (коммит `8aa1a8c`), появляется
когда мест приёма больше одного. Проверить на реальных данных нельзя, пока
валидация отклоняет корректные значения; вдобавок в базе сейчас вообще нет
врача с двумя клиниками — проверил всех 67.

## 2. `/api/clinic/doctors/` не отдаёт отзывы и стаж

**Приоритет:** высокий

Эндпоинт возвращает только `id, full_name, specialty, photo, rating,
appointments_total, is_active`. Полей `reviews_count` и `experience_years`
нет вовсе.

Из-за этого в кабинете клиники (`/clinic-profile/specialists`) карточка врача
показывает **«5.00 (0) • 0 лет опыта»**, хотя публичный `/api/doctors/201/`
для того же врача отдаёт `reviews_count: 1`. Клиника видит про своего же врача
неверные цифры — и не может понять, почему на публичной странице они другие.

**Что нужно:** добавить `reviews_count` и `experience_years` в сериализатор
`/api/clinic/doctors/` — так же, как они есть в публичном `/api/doctors/`.

## 3. Дубликаты услуг в каталоге

**Приоритет:** высокий

Из 100 услуг **8 названий задвоены**, некоторые в трёх и четырёх экземплярах:

- «Онлайн-консультация (30-40 мин)» — 4 копии
- «Прием вторичный (60 мин)» — 3 копии
- «qa_Первичный приём кардиолога», «qa_Расшифровка ЭКГ», «Прием первичный
  (60 мин)», «Массаж» — по 2 копии

Пользователь на `/services` видит подряд одинаковые карточки и не понимает,
чем они отличаются.

**Что нужно:** понять, откуда берутся копии (похоже, услуга создаётся заново
при каждом привязывании к врачу вместо переиспользования), и почистить БД.

## 4. В каталоге услуг всё ещё лица врачей

**Приоритет:** средний (было высокий)

Главное починено: услуга со своим фото теперь отдаёт именно его. Остался
фолбэк — если своего фото нет, подставляется `doctors.first().photo`.

Сейчас из 100 услуг: 1 со своим фото, 8 с фото врача, 91 без фото. На первом
экране `/services` по-прежнему все 12 карточек показывают портреты врачей —
«Первичный приём кардиолога» с чужим лицом читается как ошибка, а не как
иллюстрация.

**Что нужно:** отдавать `null`, когда своего фото нет. Фронт на `null` рисует
нормальную заглушку — инициалы услуги на фирменной подложке, — и это честнее
случайного портрета. Если фолбэк оставлен осознанно, вопрос закрыт, но тогда
это стоит зафиксировать как продуктовое решение, а не как техническую деталь.

## 5. Справочник категорий засорён названиями услуг

**Приоритет:** средний

У 7 услуг `category` дословно равна `name`: «UX» / «UX»,
«qa_Онлайн-консультация» / «qa_Онлайн-консультация», «qa_Расшифровка ЭКГ» и
так далее (id 169, 171, 172, 173, 174, 177).

В карточке из-за этого название дублируется строкой ниже, а в фильтре
«Категория услуги» появляются мусорные пункты.

**Что нужно:** почистить `category` у этих записей и не давать записывать в
справочник произвольную строку — на фронте поле уже выбирается из
`/references/service-categories/`, но старые данные остались.

## 6. Мусор в БД

**Приоритет:** средний

Клиники в публичном каталоге, которые видит любой пользователь:

- `181` — «awdadadwa»
- `182` — «adawdadaw»
- `188` — «Сизиффф»
- `200` — **«QA Visual Check Clinic»** — это мои тестовые данные, созданные во
  время проверки. Удалите вместе с пользователем
  `qa-visual-check-1787701221@example.com`. Своего эндпоинта на удаление
  аккаунта в API нет, поэтому убрать это самому я не смог.

Также остались две отменённые тестовые записи (id 34 и 35), которые не
удаляются через интерфейс.

`ITDos.Clinic` (199) и связанные с ней данные — рабочие тестовые, не трогать.

## 7. Приложение уведомлений ни от чего не наполняется

**Приоритет:** средний

`/api/notifications/` существует и отвечает, но в коде нет ни одного места,
которое бы создавало уведомление: список всегда пуст. Пока это так, показывать
их в интерфейсе нечем.

**Что нужно:** создавать уведомление на событиях, которые пользователю
действительно важны — новая запись к врачу, подтверждение/отмена записи
клиникой, новый отзыв, напоминание о приёме.

Отдельно: колокольчика в шапке не будет — это решение принято. Счётчик уже
живёт на иконке профиля и в пункте «Записи», туда же встанут и уведомления.

## 8. Филиал не подставляется и его негде взять

**Приоритет:** низкий

Клинику бэк при одной привязке проставляет сам, а филиал — нет: у услуги 184
`clinic: {id: 199}`, `branch: null`. На карточке процедуры в кабинете клиники
из-за этого остаётся «Филиал —».

Плюс филиалы негде прочитать: `GET` для них нет вообще, только
`PUT /api/clinic/branches/{id}/`. Единственный источник — массив `branches`
внутри `GET /api/clinics/{id}/`, и он сейчас пуст у всех клиник.

**Что нужно:** решить два вопроса — (1) подставлять ли единственный филиал
автоматически, как клинику; (2) оставить ли `branches` только внутри карточки
клиники. Дропдаун филиала на фронте не делал: проверить его не на чем, ни у
одной клиники филиалов нет.

## 9. У услуг нет PATCH

**Приоритет:** низкий

`/api/doctor/services/{id}/` и `/api/clinic/services/{id}/` принимают только
`PUT`. Из-за этого при правке одного поля приходится собирать тело целиком.

Само по себе это работает: проверил, что `PUT` без `photo` не стирает фото и
не сбрасывает клинику. Но контракт остаётся неочевидным — из схемы не видно,
что часть полей можно опускать.

**Что нужно:** добавить `PATCH` либо описать в схеме, какие поля необязательны
при `PUT`.

---

## Закрыто бэком

| Что было | Проверка |
| --- | --- |
| Услуги врача не получали `clinic`/`branch` | `POST` без `clinic_id` возвращает `clinic: {id: 199, name: "ITDos.Clinic"}`; на `PUT` без `clinic_id` привязка сохраняется |
| `get_photo` у услуги отдавал фото врача вместо загруженного | Услуга 184 отдаёт `/media/services/photos/…`; в таблице услуг врача фото наконец видно — это была жалоба коллеги |
| `DoctorServiceDetailView.put()` не передавал context | Правка цены из интерфейса проходит, фото и клиника переживают сохранение |
