# TASKS — что осталось доделать

> Дата ревизии: 2026-07-02 · Источники: полный аудит кода (src/ + app/), Swagger-схема,
> история коммитов, PROJECT_STATUS.md / BACKEND_TASKS.md.
>
> ⚠️ На момент ревизии бэкенд лежит (**502**, авария «диск переполнен» из выжимки f28c8d5) —
> всё, что помечено «проверить», перепроверить после подъёма сервера.
>
> Этот файл — актуальный свод. PROJECT_STATUS.md и BACKEND_TASKS.md частично устарели
> (отзывы CRUD уже реализованы с обеих сторон).

---

## Фронтенд

### P0 — сломанные пользовательские флоу (быстрые починки)

1. **Отзыв из истории записей не отправляется.**
   `src/pages/profile/history/history/ui.tsx:64` — `handleSubmitReview` = `console.log`.
   Модалка открывается, но отзыв никуда не уходит. `createReview` уже существует и
   работает на странице врача — подключить + инвалидация кэша + toast. ~30 минут работы.

2. **Редактор процедуры клиники — полностью на моках.**
   `app/clinic-profile/procedures/[id]/page.tsx` — данные из `MOCK_PROCEDURE_DATA`
   (строки 24–38), «Сохранить» (стр. 104) и «Удалить» (стр. 544) = `console.log` + redirect.
   Любые правки процедуры теряются. Переписать на react-query + реальный API по образцу
   уже переделанного `procedures/new/`. Заодно: хардкод тайм-слотов с неверным
   послеобеденным временем («01:00», «02:00» вместо 13:00+).

3. **Восстановление пароля — шаги 2–3 не работают.**
   `src/pages/forgot-password/ui.tsx` — шаг 1 (email) реально бьёт в API, но:
   ввод кода не проверяется (кнопка просто листает дальше, стр. 205),
   новый пароль **не отправляется** (стр. 267 сразу ставит `success`).
   Плюс синий квадрат-заглушка вместо иконки успеха (стр. 280).
   ⛓ Зависит от Б-4 (контракт: код vs токен-в-ссылке).

### P1 — кабинеты: API-функции готовы, UI не построен

4. **Уведомления.** `getNotifications` / `markNotificationRead` / `markAllNotificationsRead`
   реализованы, но нигде не используются. Нужен колокол в шапке + список/дропдаун.

5. **Редактор расписания врача.** `getDoctorSchedule` / `updateDoctorSchedule` готовы,
   страницы нет вообще.

6. **Записи клиники.** `getClinicAppointments` готова — в кабинете клиники нет страницы
   с записями пациентов.

7. **Отзывы клиники.** `getClinicReviews` готова — клиника не видит свои отзывы
   (у врача аналогичная страница есть).

8. **Статистика (дашборды).** `getDoctorStats` / `getClinicStats` готовы — ни один
   не подключён.

9. **Редактирование филиала.** `updateClinicBranch` готова — UI правки филиала нет.

### P2 — формы и заглушки

10. **Справочники в регистрации.** Дропдауны (города, специализации, типы клиник, языки…)
    захардкожены в `register/model/constants.ts`, хотя 7 references-эндпоинтов реализованы.
    ⛓ Сначала Б-8 (подтверждение контракта имён).

11. **Шаг 6 регистрации врача отправляется пустым** (оборудование/условия/оплата) —
    проверить маппинг формы → запроса и починить.

12. **Процедуры: фото, расписание, привязка к филиалу.**
    `procedures/new/page.tsx` (стр. 308–324): «в разработке» — фото не грузится,
    расписание не сохраняется, к филиалу не привязывается. ⛓ Частично зависит от Б-6.

13. **Геолокация города — фейк.** `src/widgets/header/ui/city-selector.tsx:69-99` —
    координаты берутся и логируются, но через 1 сек всегда ставится «Бишкек».
    Нужен геокодинг-сервис (2GIS/Яндекс/Google + ключ).

14. **«Проверить статус» оплаты — фейковый спиннер на 3 сек.**
    `src/pages/record/ui/SummaryCard.tsx:23-26`. ⛓ Ждёт Б-10 (GET одной записи).

### P3 — техдолг, перф, бэклог

15. **web-vitals (RUM).** Лабораторные замеры сделаны (все страницы 87–94 mobile),
    но реальных полевых метрик с устройств пользователей нет — добавить `web-vitals`
    и отправку на бэк/аналитику. Особенно важно для Capacitor WebView.

16. **/record: TBT 260ms** (чуть выше порога 200ms) — отложить тяжёлые части формы.
    ⏸ По договорённости пока не трогаем.

17. **Дубль `getDoctorReviews`** — страница отзывов врача использует общий `getReviews`;
    либо переключиться, либо удалить функцию.

18. **Категории блога** — фильтр работает на локальных константах, `getBlogCategories`
    не подключён.

19. **Бэклог продукта** (зафиксировано в f28c8d5, решения по продукту нет):
    соцвход (Google/Apple), карта при выборе города, выбор клиники в форме регистрации врача.

---

## Бэкенд

### P0 — блокеры

1. 🚨 **Сервер лежит — 502** (диск базы данных переполнен). Блокирует вообще всё:
   каталог пуст, кабинеты не работают, SSR отдаёт пустые списки. Поднять + добавить
   мониторинг диска.

2. **`GET /api/clinics/` пуст при любых параметрах** (`data: []`). Либо баг фильтрации,
   либо нет данных — из-за этого страница «Клиники» всегда «не найдено».

3. **Фильтры `/api/doctors/` сломаны/неполны:**
   - `specialization` возвращает 0 результатов даже при точном совпадении (проверено
     прямыми запросами) — фронт вынужден фильтровать на клиенте;
   - параметра по **стажу** нет вообще.

### P1 — блокируют готовые экраны фронта

4. **Password reset: определиться с контрактом подтверждения.**
   Бэк принимает `{token, password}` (токен из письма), макет и текущий UI ждут
   **4-значный код**. Либо добавить код-флоу
   (`POST /api/auth/password-reset/confirm/` с кодом), либо согласуем токен-в-ссылке —
   тогда фронт переделает шаги 2–3 (Ф-3).

5. **`ServiceListItem` без полей `rating` / `reviews_count` / `photo` / `clinic`.**
   Из-за этого на «Услугах» фильтры «Оценка» и «Клиника» скрыты с пометкой «скоро»,
   а карточки фабрикуют нули на фронте.

6. **`documents[]` и `photos[]` не существуют ни в одной схеме.**
   Блоки «Фотографии» и «Юридическая информация» клиники и «Сертификаты» врача стоят
   с надписью «в разработке» — файл-пикеры готовы, сохранять некуда.
   Нужно: `documents: string[]` / `photos: string[]` (URL) в профилях/регистрации
   или отдельные эндпоинты (детали в BACKEND_TASKS.md §9).

7. **`patronymic` (отчество) в профиле пациента.** Поле есть в UI, но в схеме
   профиля его нет — сейчас хранится только в localStorage браузера (теряется при
   смене устройства).

### P2 — контракт и будущее

8. **Справочники: подтвердить, что регистрация/фильтры принимают имена**
   (`"Бишкек"`, `"Частная"`) ровно как отдают `/api/references/*`. Разблокирует Ф-10.

9. **Файлы в регистрации — принимать URL-строки** (фронт грузит через
   `POST /api/upload/` заранее): `step1.photo` врача, `logo` клиники.

10. **`GET /api/appointments/{id}/`** — получение одной записи (статус, оплата,
    `google_meet_link`). Разблокирует Ф-14. Не срочно, пока нет платежей.

11. **`lunch_break` и `emergency_24_7` в `GET /api/clinic/profile/`** — форма
    регистрации их собирает, но профиль не возвращает: кабинет показывает пустые
    значения по умолчанию.

### ✅ Сделано бэком с прошлой ревизии (проверить после подъёма сервера)

- **Отзывы CRUD** — `POST/PUT/DELETE /api/reviews/` + reply: фронт уже подключил
  создание (страница врача), правку/удаление/ответ (`/profile/reviews`, виджет отзывов).
- **`POST /api/auth/password-reset/`** — существует, шаг 1 фронта подключён.

---

### Сводка

| Блок | P0 | P1 | P2 | P3 | Всего |
|---|---|---|---|---|---|
| Фронтенд | 3 | 6 | 5 | 5 | 19 |
| Бэкенд | 3 | 4 | 4 | — | 11 |
