# TASKS — что осталось доделать

> Дата ревизии: 2026-07-02 (обновлено после подъёма бэка) · Источники: полный аудит кода
> (src/ + app/), живая Swagger-схема (65 эндпоинтов), живые запросы к API, история коммитов.
>
> ✅ Бэкенд поднят после аварии с диском и **заметно доработан**: появился полный код-флоу
> восстановления пароля, `GET /api/appointments/{id}/`, `lunch_break`/`emergency_24_7`
> в профилях, отзывы CRUD подтверждены. Всё «живое» ниже проверено запросами 2026-07-02.
>
> Этот файл — актуальный свод. PROJECT_STATUS.md и BACKEND_TASKS.md устарели.

---

## Фронтенд

### P0 — сломанные пользовательские флоу (быстрые починки)

1. **Отзыв из истории записей не отправляется.**
   `src/pages/profile/history/history/ui.tsx:64` — `handleSubmitReview` = `console.log`.
   Модалка открывается, но отзыв никуда не уходит. `createReview` уже существует и
   работает на странице врача; бэк принимает `appointment_id` в `POST /api/reviews/` —
   привязать отзыв к записи. ~30 минут работы.

2. **Редактор процедуры клиники — полностью на моках.**
   `app/clinic-profile/procedures/[id]/page.tsx` — данные из `MOCK_PROCEDURE_DATA`
   (строки 24–38), «Сохранить» (стр. 104) и «Удалить» (стр. 544) = `console.log` + redirect.
   Любые правки процедуры теряются. Переписать на react-query + реальный API по образцу
   уже переделанного `procedures/new/`. Заодно: хардкод тайм-слотов с неверным
   послеобеденным временем («01:00», «02:00» вместо 13:00+).

3. **Восстановление пароля — шаги 2–3 не работают.** ✅ РАЗБЛОКИРОВАНО бэком.
   `src/pages/forgot-password/ui.tsx` — шаг 1 (email) реально бьёт в API, но:
   ввод кода не проверяется (кнопка просто листает дальше, стр. 205),
   новый пароль **не отправляется** (стр. 267 сразу ставит `success`).
   Бэк теперь отдаёт ровно тот флоу, что в макете:
   `POST /api/auth/password-reset/verify/` `{email, code}` и
   `POST /api/auth/password-reset/confirm/` `{email, code, password}` — подключить.
   Плюс синий квадрат-заглушка вместо иконки успеха (стр. 280).

### P1 — кабинеты: API готов, UI не построен

4. **Уведомления.** `getNotifications` / `markNotificationRead` / `markAllNotificationsRead`
   реализованы, но нигде не используются. Нужен колокол в шапке + список/дропдаун.

5. **Редактор расписания врача.** `getDoctorSchedule` / `updateDoctorSchedule` готовы,
   страницы нет вообще. ⚠️ Сам по себе запись не разблокирует: график, заданный при
   регистрации, бэк уже хранит, но слоты из него не генерирует (см. Б-1) — сначала
   бэк чинит генерацию, редактор нужен следом (менять график после регистрации
   врач сейчас не может никак).

6. **Записи клиники.** `getClinicAppointments` готова — в кабинете клиники нет страницы
   с записями пациентов.

7. **Отзывы клиники.** `getClinicReviews` готова — клиника не видит свои отзывы
   (у врача аналогичная страница есть).

8. **Статистика (дашборды).** `getDoctorStats` / `getClinicStats` готовы — ни один
   не подключён.

9. **Редактирование филиала.** `updateClinicBranch` готова — UI правки филиала нет.

10. **Итоги приёма (новое на бэке).** Появился `GET/PATCH /api/doctor/appointments/{id}/summary/` —
    врач может заполнять итоги/заключение по записи. В API-слое и UI ещё нет ничего.

22. ✅ **СДЕЛАНО 2026-07-04 (фронтовая часть): кнопка «Написать».**
    `features/start-chat` → кнопка на странице врача (рядом с «Записаться»,
    десктоп + мобильный бар) и «Написать врачу» в карточках истории записей;
    `/chat?room=<id>` открывает диалог сразу. Гость уводится на логин.
    Осталось на стороне бэка (опционально, для лучшего UX): **автосоздание
    комнаты при записи** — проверено экспериментом, что сейчас запись комнату
    НЕ создаёт (запись #6 создана — комнат 0 у обеих сторон).
    ⛓ Реальную переписку всё ещё блокирует Б-12 (WebSocket не развёрнут).

### P2 — формы и заглушки

11. **Справочники в регистрации.** Дропдауны (города, специализации, типы клиник, языки…)
    захардкожены в `register/model/constants.ts`, хотя references-эндпоинты реализованы.
    ⛓ Сначала Б-9 (подтверждение контракта имён).

12. **Шаг 6 регистрации врача отправляется пустым** (оборудование/условия/оплата) —
    проверить маппинг формы → запроса и починить.

13. **Обед и 24/7 в кабинете клиники.** ✅ РАЗБЛОКИРОВАНО бэком: `lunch_break` и
    `emergency_24_7` появились в `ClinicOwnProfile` (и в `DoctorOwnProfile`).
    Сейчас `useClinicCabinet.mapWorkSchedule` ставит им пустые значения по умолчанию —
    смаппить реальные и включить редактирование.

14. **«Проверить статус» оплаты — фейковый спиннер на 3 сек.** ✅ РАЗБЛОКИРОВАНО бэком:
    `GET /api/appointments/{id}/` теперь существует. Подключить реальный опрос статуса
    в `src/pages/record/ui/SummaryCard.tsx:23-26`.

15. **Процедуры: фото, расписание, привязка к филиалу.**
    `procedures/new/page.tsx` (стр. 308–324): «в разработке» — фото не грузится,
    расписание не сохраняется, к филиалу не привязывается. ⛓ Частично зависит от Б-7.

16. **Геолокация города — фейк.** `src/widgets/header/ui/city-selector.tsx:69-99` —
    координаты берутся и логируются, но через 1 сек всегда ставится «Бишкек».
    Нужен геокодинг-сервис (2GIS/Яндекс/Google + ключ).

### P3 — техдолг, перф, бэклог

17. **Блог вообще не подключён к API.** `src/entities/blog/model.ts:243` —
    `fetchBlogPosts`/`fetchBlogCategories` возвращают локальные константы `BLOG_POSTS`,
    статьи тоже статичные. При этом `/api/blog/` жив, но пуст (`data: []`).
    ⛓ Сначала Б-11 (наполнить контентом), потом подключить список+статью+категории.

18. **web-vitals (RUM).** Лабораторные замеры сделаны (все страницы 87–94 mobile),
    но реальных полевых метрик с устройств пользователей нет — добавить `web-vitals`
    и отправку на бэк/аналитику. Особенно важно для Capacitor WebView.

19. **/record: TBT 260ms** (чуть выше порога 200ms) — отложить тяжёлые части формы.
    ⏸ По договорённости пока не трогаем.

20. **Дубль `getDoctorReviews`** — страница отзывов врача использует общий `getReviews`;
    либо переключиться, либо удалить функцию.

21. **Бэклог продукта** (зафиксировано в f28c8d5, решения по продукту нет):
    соцвход (Google/Apple), карта при выборе города, выбор клиники в форме регистрации врача.

---

## Бэкенд

### P0 — блокеры основного флоу (подтверждено живыми запросами 2026-07-02)

1. ✅ **ИСПРАВЛЕНО 2026-07-04: генерация слотов работает.**
   Перепроверено: у врачей 270 и 272 `available-slots` отдаёт слоты по графику,
   занятые помечаются `available: false`. Запись через `/record` прошла
   end-to-end (запись #4, 06.07 14:30, Google Meet-ссылка выдана).
   ~~Слоты записи не генерируются — запись к врачу невозможна вообще~~
   (репро было: график сохранён, профиль опубликован, услуга есть — слоты пустые).

12. 🚨 **WebSocket чата не работает — переписка невозможна (добавлено 2026-07-04).**
    REST-часть чата живая (комнаты создаются/листятся, история отдаётся), но
    сокет не подключается ни у пациента, ни у врача:
    - `wss://imbir.sino0on.ru/ws/chat/2/?token=<валидный access>` → обрыв
      **1006** без close-фрейма (хендшейк не проходит);
    - GET с заголовками `Upgrade: websocket` на тот же путь → **HTTP 404
      text/html от Django** (заголовки X-Frame-Options/Referrer-Policy — это
      джанговский ответ, не nginx).
    → По контрактному адресу `/ws/chat/{room_id}/?token=…` вебсокет-эндпоинта
    нет: либо не запущен ASGI-сервер (daphne/uvicorn + Channels; сейчас,
    похоже, только WSGI под REST), либо nginx не проксирует upgrade
    (`proxy_set_header Upgrade/Connection` для `location /ws/`), либо путь
    в ws-роутинге отличается от ТЗ. Проверял и из браузера (фронт), и напрямую
    из Node — одинаково. Не зависит от времени записи (проверено).

2. **Новые врачи невидимы: `is_published: false` по умолчанию, и непубличный
   врач отдаёт 404 даже по прямому id.** Кто/когда должен публиковать профиль?
   Если модерации нет — авто-публиковать после регистрации или дать фронту
   тумблер (поле писабельно через `PUT /api/doctor/profile/`).
   Заодно: `PATCH /api/doctor/profile/` → **405**, работает только `PUT` —
   либо включить PATCH, либо убрать его из схемы.

3. **Тестовые аккаунты пропали после аварии с диском** — логин по кредам из
   `acc.md` даёт «Неверный email или пароль». Похоже, база откатилась/чистилась.
   Уточнить, что с данными; команды в `acc.md` обновлены (новый тестовый врач).

4. **`GET /api/clinics/` пуст при любых параметрах** (`data: []`, total 0).
   Либо баг фильтрации/публикации, либо нет данных — страница «Клиники» всегда
   «не найдено». Если дело в `is_published` — уточнить условия публикации.

5. **Фильтры `/api/doctors/` сломаны/неполны:**
   - `?specialization=Уролог` → 0 результатов, хотя `?search=Азамат` находит этого
     уролога (проверено сегодня) — фильтр не работает;
   - параметра по **стажу** (experience) нет вообще — фронт фильтрует на клиенте.

### P1 — блокируют готовые экраны фронта

6. **`ServiceList` без полей `rating` / `reviews_count` / `photo` / `clinic`.**
   Сейчас `{id, name, category, price, duration}` (подтверждено по схеме).
   Из-за этого на «Услугах» фильтры «Оценка» и «Клиника» скрыты с пометкой «скоро»,
   а карточки фабрикуют нули на фронте.

7. **`documents[]` и `photos[]` не существуют ни в одной схеме** (проверено:
   `ClinicOwnProfile`, `DoctorOwnProfile`, регистрация). Блоки «Фотографии» и
   «Юридическая информация» клиники и «Сертификаты» врача стоят с надписью
   «в разработке» — файл-пикеры готовы, сохранять некуда.
   Нужно: `documents: string[]` / `photos: string[]` (URL) или отдельные эндпоинты.

8. **`patronymic` (отчество) в профиле пациента.** Поле есть в UI, в `PatientProfile`
   его нет (подтверждено) — хранится только в localStorage браузера.

### P2 — контракт и данные

9. **Справочники: подтвердить, что регистрация/фильтры принимают имена**
   (`"Бишкек"`, `"Частная"`) ровно как отдают `/api/references/*`. Разблокирует Ф-11.

10. **Файлы в регистрации — принимать URL-строки** (фронт грузит через
    `POST /api/upload/` заранее): `step1.photo` врача, `logo` клиники.

11. **Наполнить блог.** `/api/blog/` жив, но `data: []` — без контента фронту нечего
    подключать (Ф-17). Уточнить: контент будет через админку или сидинг?

### ✅ Сделано бэком — подтверждено по живой схеме 2026-07-02

- **Сервер поднят** после аварии с диском (все эндпоинты 200). Рекомендация:
  мониторинг диска, чтобы 502 не повторился.
- **Отзывы CRUD целиком**: `POST /api/reviews/` (с `target_type`/`target_id`/`appointment_id`),
  `PUT/PATCH/DELETE /api/reviews/{id}/`, `POST /api/reviews/{id}/reply/`.
  Фронт уже подключил всё, кроме отзыва из истории (Ф-1).
- **Восстановление пароля — полный код-флоу**: `password-reset/` → `verify/ {email, code}`
  → `confirm/ {email, code, password}` — ровно как в макете. Ждёт фронт (Ф-3).
- **`GET /api/appointments/{id}/`** — есть (разблокирует Ф-14).
- **`lunch_break` + `emergency_24_7`** в `ClinicOwnProfile` и `DoctorOwnProfile`
  (разблокирует Ф-13).
- **Новое: `GET/PATCH /api/doctor/appointments/{id}/summary/`** — итоги приёма (→ Ф-10).

---

### Сводка

| Блок | P0 | P1 | P2 | P3 | Всего |
|---|---|---|---|---|---|
| Фронтенд | 3 | 8 | 6 | 5 | 22 |
| Бэкенд | 5 (Б-1 ✅ решён) | 3 | 3 | — | 11 |

Из 22 фронтовых задач **4 разблокированы правками бэка** (Ф-3, Ф-10, Ф-13, Ф-14).
~~Самое срочное для бэка — Б-1 (генерация слотов)~~ — **исправлено 2026-07-04,
запись работает end-to-end**. Теперь самое срочное — **Б-12 (WebSocket чата:
1006/404, переписка невозможна)**, дальше Б-2 (невидимость новых врачей)
и Б-4 (пустой список клиник).

**Вопрос продукту** (всплыл при проверке записи): авторизованный пользователь не
может записать другого человека — введённые в форму контакты не отправляются
(запись привязывается к JWT). Если сценарий «записать родственника» нужен, бэк
должен принимать `guest_*` и от авторизованных.
