// Наборы по умолчанию для справочников бэка (см. shared/lib/useReference):
// показываются, пока справочник грузится, и дополняют его, если он неполный.
// Общие для формы регистрации и профиля клиники — единственный источник,
// чтобы значения не расходились между двумя местами редактирования одних и
// тех же данных.
export const DEFAULT_CLINIC_TYPES = [
  "Частная",
  "Государственная",
  "Многопрофильная",
  "Специализированная",
];

// Страны берём из справочника телефонных кодов (там же человеческие
// названия); этот список — только на случай, если справочник не ответил.
export const DEFAULT_COUNTRIES = ["Кыргызстан", "Казахстан", "Россия"];
